import BaseLevel from './baseLevel';
import { SceneKey } from './index';

export default class Level5 extends BaseLevel {
	constructor() {
		super(SceneKey.Level5, SceneKey.PreloadLevel1, 'level5');
	}

	create() {
		super.create();
	}

	update() {
		super.update();
	}
}