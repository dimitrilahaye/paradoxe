import BaseLevel from './baseLevel';
import { SceneKey } from './index';

export default class Level4 extends BaseLevel {
	constructor() {
		super(SceneKey.Level4, SceneKey.PreloadLevel1, 'level4');
	}

	create() {
		super.create();
	}

	update() {
		super.update();
	}
}